const LoadingIcon = () => {
  return (
    <div
      className='loader'
      style={{
        position: 'absolute',
        right: '550px',
        top: '350px',
        zIndex: 1000000,
      }}
    ></div>
  );
};

export default LoadingIcon;
